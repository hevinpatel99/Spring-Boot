server:
  port: 8084

  #  servlet:
  #    context-path: /employee_management_system
  #

  ######### Mysql Config ########
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/jpa_projection?createDatabaseIfNotExist=true&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  # jpa properties
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
